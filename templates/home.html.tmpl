{{define "content"}}
<div class="container">
  <div class="row">
    <div class="jumbotron">
      <h1>Hello, Worlds!</h1>
      <p>Welcome to your new Go web project.</p>
    </div>
    
    {{range .}}
      <div>
        {{.Title}}
      </div>
    {{end}}

    
  </div>
</div>
{{end}}
